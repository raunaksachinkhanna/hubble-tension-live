// Single source of truth for family colors, used by the chart and the legend.
export const EARLY_UNIVERSE_COLOR = "#3366cc"; // e.g. CMB-based methods (Planck)
export const LATE_UNIVERSE_COLOR = "#cc6633"; // e.g. distance-ladder methods (SH0ES, CCHP)

export const FAMILY_COLORS = {
  early_universe: EARLY_UNIVERSE_COLOR,
  late_universe: LATE_UNIVERSE_COLOR,
};

export const FAMILY_LABELS = {
  early_universe: "Early universe",
  late_universe: "Late universe",
};
